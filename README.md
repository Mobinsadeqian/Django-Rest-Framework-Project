📚 سیستم مدیریت کتابخانه (Library Management API)

یک API کامل و استاندارد برای مدیریت کتابخانه شخصی که با استفاده از Django و Django REST Framework (DRF) توسعه داده شده است.

این پروژه به عنوان یک نمونه کار برای نمایش مفاهیم کلیدی توسعه API از جمله CRUD، احراز هویت، سطوح دسترسی (Permissions) و مستندسازی خودکار طراحی شده است.

🚀 ویژگی‌های کلیدی

مدیریت کامل کتاب‌ها (CRUD): ایجاد، خواندن، بروزرسانی و حذف کتاب‌ها.

معماری استاندارد: استفاده از ViewSets و Routers برای کدنویسی تمیز و استاندارد.

سیستم احراز هویت و مجوزها (Auth & Permissions):

استفاده از سیستم احراز هویت جنگو.

پیاده‌سازی Custom Permission (IsOwnerOrReadOnly)؛ هر کاربر فقط می‌تواند پست‌های خودش را ویرایش یا حذف کند.

جستجو و فیلترینگ: قابلیت جستجو در عنوان و نویسنده کتاب‌ها و مرتب‌سازی بر اساس تاریخ انتشار.

صفحه‌بندی (Pagination): مدیریت نمایش داده‌ها با صفحه‌بندی استاندارد برای بهینه‌سازی عملکرد.

مستندات تعاملی (Swagger UI): داکیومنت کامل و قابل تست API با استفاده از drf-spectacular.

🛠 تکنولوژی‌های استفاده شده

Language: Python 3.x

Framework: Django 5.x

API Toolkit: Django REST Framework (DRF)

Documentation: drf-spectacular (Swagger/Redoc)

Database: SQLite (قابل تغییر به PostgreSQL)

📦 نحوه نصب و اجرا

۱. پروژه را کلون کنید:

git clone [https://github.com/username/library-management-api.git](https://github.com/username/library-management-api.git)
cd library-management-api


۲. یک محیط مجازی (Virtual Environment) بسازید و فعال کنید:

python -m venv venv
# در ویندوز:
venv\Scripts\activate
# در مک/لینوکس:
source venv/bin/activate


۳. پیش‌نیازها را نصب کنید:

pip install django djangorestframework drf-spectacular


۴. مایگریشن‌ها را اعمال کنید:

python manage.py migrate


۵. یک سوپریوزر بسازید (برای دسترسی به پنل ادمین):

python manage.py createsuperuser


۶. سرور را اجرا کنید:

python manage.py runserver


📖 مستندات API

بعد از اجرای سرور، می‌توانید به آدرس‌های زیر بروید تا مستندات کامل را ببینید و API را تست کنید:

Swagger UI: http://127.0.0.1:8000/api/docs/

Redoc: http://127.0.0.1:8000/api/redoc/

🔗 لیست اندپوینت‌های اصلی

متد

آدرس (Endpoint)

توضیحات

سطح دسترسی

GET

/api/books/

لیست تمام کتاب‌ها

عمومی

POST

/api/books/

ایجاد کتاب جدید

نیاز به لاگین

GET

/api/books/{id}/

مشاهده جزئیات یک کتاب

عمومی

PUT

/api/books/{id}/

ویرایش کامل کتاب

فقط صاحب پست

PATCH

/api/books/{id}/

ویرایش جزئی کتاب

فقط صاحب پست

DELETE

/api/books/{id}/

حذف کتاب

فقط صاحب پست

👤 نویسنده

توسعه داده شده توسط مبین
